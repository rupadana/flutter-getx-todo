import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

const double BORDER_RADIUS = 10;
const double PADDING_FORM_ITEM_VERTICAL = 10;
const double PADDING_BODY = 10;
const double PADDING_PENGUMUMAN = 20;
const double PADDING_SIGNATURE = 10;

final BOX_SHADOW = BoxShadow(
  color: Colors.grey.withOpacity(0.3),
  spreadRadius: 1,
  blurRadius: 2,
  offset: Offset(0, 0), // changes position of shadow
);
